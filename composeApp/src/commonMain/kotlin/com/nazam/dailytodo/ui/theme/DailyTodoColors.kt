package com.nazam.dailytodo.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette PRO (neutre + bleu)
 * - Fond: gris très clair / blanc
 * - Texte: noir/bleu nuit
 * - Accent: bleu moderne (pas de violet)
 */
object DailyTodoColors {

    // Neutrals (Light)
    val Bg = Color(0xFFF6F8FB)
    val Surface = Color(0xFFFFFFFF)
    val Surface2 = Color(0xFFF0F3F8)
    val Outline = Color(0xFFE3E8F2)

    // Text (Light)
    val TextPrimary = Color(0xFF0B1220)
    val TextSecondary = Color(0xFF556070)

    // Accent Blue (Modern)
    val Blue = Color(0xFF1F5BFF)
    val BlueSoft = Color(0xFF12B3FF)

    // Dark
    val DarkBg = Color(0xFF0B0F14)
    val DarkSurface = Color(0xFF111827)
    val DarkSurface2 = Color(0xFF161F2D)
    val DarkOutline = Color(0xFF2A3445)
    val DarkTextSecondary = Color(0xFFA7B0C0)

    // Status
    val Error = Color(0xFFB91C1C)
    val White = Color(0xFFFFFFFF)
    val Black = Color(0xFF000000)
}
